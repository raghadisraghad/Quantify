# Eureka -  discovery service

## Environment file

``` bash
cp .env.example .env
```

## Runtime

this springboot application uses JDK 25

run the application and it will install all necessary dependencies on its own