spring.application.name=api_gateway_service
server.port=${SERVER_PORT}

# Eureka
eureka.client.service-url.defaultZone=http://${EUREKA_HOST}:${EUREKA_PORT}/eureka
eureka.instance.prefer-ip-address=true
eureka.instance.hostname=${EUREKA_HOST}

# Gateway routes using Eureka service names
spring.cloud.gateway.server.webflux.discovery.locator.enabled=true

spring.cloud.gateway.server.webflux.routes[0].id=INVENTORY_SERVICE
spring.cloud.gateway.server.webflux.routes[0].uri=http://${EUREKA_HOST}:8082
spring.cloud.gateway.server.webflux.routes[0].predicates[0]=Path=/inventory/**

spring.cloud.gateway.server.webflux.discovery.locator.lower-case-service-id=true

# Logging
logging.level.org.springframework.cloud.gateway=DEBUG
logging.level.com.netflix.discovery=DEBUG